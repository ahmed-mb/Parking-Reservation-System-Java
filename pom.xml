<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <!--
            Bumped from 3.2.0 (Nov 2023) during the CI hardening pass to pick
            up patched transitive dependencies for the Trivy gate. This single
            version change pulls in fixed versions of tomcat-embed-core,
            spring-security-*, spring-web*, and logback-* and clears the
            stop-ship CVEs surfaced by the production image scan.
            Dependabot will continue bumping this minor as patches land.
        -->
        <version>3.4.5</version>
        <relativePath/> <!-- lookup parent from repository -->
    </parent>
    <groupId>com.ahmedbahaj</groupId>
    <artifactId>parking-reservation-system</artifactId>
    <version>0.0.1-SNAPSHOT</version>
    <name>Parking Reservation System</name>
    <description>Parking System migrated to Java Spring Boot</description>

    <properties>
        <java.version>17</java.version>
        <!-- Pinned versions for plugins added in the production-readiness pass.
             Bumping these is a deliberate operation; Dependabot will open PRs. -->
        <testcontainers.version>1.20.4</testcontainers.version>
        <cyclonedx.version>2.8.0</cyclonedx.version>
        <license.plugin.version>2.7.1</license.plugin.version>

        <!--
            CVE-driven transitive overrides.

            Spring Boot's BOM exposes these properties so application POMs
            can override the managed version of high-traffic third-party
            components without forking the BOM. The values here are pinned
            to the smallest patch version that closes the CRITICAL CVEs
            surfaced by the Trivy gate against Spring Boot 3.4.5's defaults:

              CVE-2026-29145  Tomcat auth bypass (CLIENT_CERT soft-fail)
              CVE-2026-41293  Tomcat HTTP/2 request headers not validated
              CVE-2026-43512  Tomcat auth bypass (digest authentication)
              CVE-2026-43515  Tomcat improper authorization / security bypass
                              -> tomcat 10.1.55+
              CVE-2025-41232  Spring Security method-security bypass
                              -> spring-security 6.4.6+ (covered)
              CVE-2026-22732  Spring Security policy bypass / disclosure
                              -> spring-security 6.5.9+

            Dependabot keeps these aligned with upstream patch releases;
            once Spring Boot's own BOM catches up, the overrides become
            no-ops and can be removed in the same PR that bumps the
            parent version.
        -->
        <tomcat.version>10.1.55</tomcat.version>
        <spring-security.version>7.1.0</spring-security.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <!-- Testcontainers BOM keeps mssqlserver / junit-jupiter in lock-step. -->
            <dependency>
                <groupId>org.testcontainers</groupId>
                <artifactId>testcontainers-bom</artifactId>
                <version>${testcontainers.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <!--
                CVE-driven BOM imports and explicit pins.

                The property-based override above (<tomcat.version>,
                <spring-security.version>) is what the Spring Boot docs
                recommend, but the property names vary slightly across
                Spring Boot versions and don't always take effect. The
                entries below are the belt-and-suspenders: an explicit
                BOM import for Spring Security plus pinned versions for
                the three Tomcat embed artifacts. With these in place,
                the Maven dependency tree resolves the patched versions
                regardless of which property name the parent BOM honours.

                These exist solely to close the CVEs surfaced by the
                Trivy gate (see <properties> for the CVE list). Remove
                them once the parent Spring Boot version naturally
                ships the patched transitives.
            -->
            <dependency>
                <groupId>org.springframework.security</groupId>
                <artifactId>spring-security-bom</artifactId>
                <version>${spring-security.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <dependency>
                <groupId>org.apache.tomcat.embed</groupId>
                <artifactId>tomcat-embed-core</artifactId>
                <version>${tomcat.version}</version>
            </dependency>
            <dependency>
                <groupId>org.apache.tomcat.embed</groupId>
                <artifactId>tomcat-embed-el</artifactId>
                <version>${tomcat.version}</version>
            </dependency>
            <dependency>
                <groupId>org.apache.tomcat.embed</groupId>
                <artifactId>tomcat-embed-websocket</artifactId>
                <version>${tomcat.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>
        <dependency>
            <groupId>com.microsoft.sqlserver</groupId>
            <artifactId>mssql-jdbc</artifactId>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.security</groupId>
            <artifactId>spring-security-test</artifactId>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>com.h2database</groupId>
            <artifactId>h2</artifactId>
            <scope>runtime</scope>
        </dependency>

        <!-- Spring Security -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-security</artifactId>
        </dependency>

        <!-- JWT -->
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-api</artifactId>
            <version>0.12.3</version>
        </dependency>
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-impl</artifactId>
            <version>0.13.0</version>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-jackson</artifactId>
            <version>0.12.3</version>
            <scope>runtime</scope>
        </dependency>

        <!-- Swagger/OpenAPI -->
        <dependency>
            <groupId>org.springdoc</groupId>
            <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
            <version>2.2.0</version>
        </dependency>

        <!-- Spring Boot Actuator -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>

        <!-- =================================================================
             Test-scoped: Testcontainers for SQL Server integration tests.
             These are pulled at compile-time of *test* code only and never
             ship in the runtime JAR. The integration test classes are named
             *IT.java so they bind to the failsafe / verify phase, not the
             unit-test surefire / test phase.
             ============================================================= -->
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>mssqlserver</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <excludes>
                        <exclude>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                        </exclude>
                    </excludes>
                </configuration>
            </plugin>

            <!-- =============================================================
                 Failsafe: runs integration tests (*IT.java) during the
                 verify phase. Surefire (which is configured by the parent)
                 continues to run *Test.java unit tests during the test phase.
                 ========================================================= -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-failsafe-plugin</artifactId>
                <executions>
                    <execution>
                        <goals>
                            <goal>integration-test</goal>
                            <goal>verify</goal>
                        </goals>
                    </execution>
                </executions>
                <configuration>
                    <includes>
                        <include>**/*IT.java</include>
                    </includes>
                </configuration>
            </plugin>

            <!-- =============================================================
                 JaCoCo Code Coverage. The 95% line-coverage gate is preserved
                 from before the audit; new prod code (RateLimitingFilter,
                 UserResponse, the new exception handlers) is covered by the
                 tests added in this pass.
                 ========================================================= -->
            <plugin>
                <groupId>org.jacoco</groupId>
                <artifactId>jacoco-maven-plugin</artifactId>
                <version>0.8.11</version>
                <executions>
                    <execution>
                        <goals>
                            <goal>prepare-agent</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>report</id>
                        <phase>test</phase>
                        <goals>
                            <goal>report</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>check</id>
                        <phase>test</phase>
                        <goals>
                            <goal>check</goal>
                        </goals>
                        <configuration>
                            <rules>
                                <rule>
                                    <element>BUNDLE</element>
                                    <limits>
                                        <limit>
                                            <counter>LINE</counter>
                                            <value>COVEREDRATIO</value>
                                            <minimum>0.95</minimum>
                                        </limit>
                                    </limits>
                                </rule>
                            </rules>
                        </configuration>
                    </execution>
                </executions>
                <configuration>
                    <excludes>
                        <exclude>**/ParkingSystemApplication.class</exclude>
                        <exclude>**/dto/**</exclude>
                        <exclude>**/model/**</exclude>
                        <exclude>**/exception/*Exception.class</exclude>
                        <exclude>**/config/OpenApiConfig.class</exclude>
                        <exclude>**/config/WebConfig.class</exclude>
                    </excludes>
                </configuration>
            </plugin>

            <!-- =============================================================
                 CycloneDX SBOM. Emits target/bom.json at the package phase
                 listing every compile + runtime dependency with version,
                 license, and a SHA-256 content hash. This is the artefact
                 B2B procurement teams will request, and the input format
                 most supply-chain scanners (Trivy, Grype, Dependency-Track)
                 ingest natively.
                 ========================================================= -->
            <plugin>
                <groupId>org.cyclonedx</groupId>
                <artifactId>cyclonedx-maven-plugin</artifactId>
                <version>${cyclonedx.version}</version>
                <executions>
                    <execution>
                        <id>make-sbom</id>
                        <phase>package</phase>
                        <goals>
                            <goal>makeAggregateBom</goal>
                        </goals>
                    </execution>
                </executions>
                <configuration>
                    <projectType>application</projectType>
                    <schemaVersion>1.5</schemaVersion>
                    <includeBomSerialNumber>true</includeBomSerialNumber>
                    <includeCompileScope>true</includeCompileScope>
                    <includeProvidedScope>true</includeProvidedScope>
                    <includeRuntimeScope>true</includeRuntimeScope>
                    <includeSystemScope>true</includeSystemScope>
                    <includeTestScope>false</includeTestScope>
                    <includeLicenseText>false</includeLicenseText>
                    <outputFormat>json</outputFormat>
                    <outputName>bom</outputName>
                </configuration>
            </plugin>

            <!-- =============================================================
                 License gate. Walks the full transitive dependency tree at
                 the verify phase and FAILS THE BUILD if anything matches a
                 banned license. The banned set is GPL/AGPL/SSPL — licences
                 that would force this product to be re-licensed if shipped
                 to a paying customer.
                 The plugin also emits target/generated-sources/license/
                 THIRD-PARTY.txt, which is uploaded as a CI artefact for the
                 buyer's legal review.
                 ========================================================= -->
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>license-maven-plugin</artifactId>
                <version>${license.plugin.version}</version>
                <executions>
                    <execution>
                        <id>aggregate-third-party-report</id>
                        <phase>verify</phase>
                        <goals>
                            <goal>aggregate-add-third-party</goal>
                        </goals>
                        <configuration>
                            <failIfWarning>false</failIfWarning>
                            <failOnMissing>false</failOnMissing>
                            <includedScopes>compile,runtime</includedScopes>
                            <excludedScopes>test,provided,system</excludedScopes>
                            <useMissingFile>false</useMissingFile>
                        </configuration>
                    </execution>
                    <execution>
                        <id>fail-on-banned-licenses</id>
                        <phase>verify</phase>
                        <goals>
                            <goal>aggregate-download-licenses</goal>
                        </goals>
                        <configuration>
                            <failOnBlacklist>true</failOnBlacklist>
                            <licensesOutputDirectory>${project.build.directory}/third-party-licenses</licensesOutputDirectory>
                            <licensesOutputFile>${project.build.directory}/third-party-licenses/licenses.xml</licensesOutputFile>
                            <includedScopes>compile,runtime</includedScopes>
                            <excludedScopes>test,provided,system</excludedScopes>
                            <!--
                                Regex of licence names that are banned for
                                redistribution under a commercial sale.
                                If a transitive dep ever pulls one in, this
                                phase will fail at `mvn verify` time, before
                                a release ever leaves CI.
                            -->
                            <excludedLicenses>
                                <excludedLicense>GNU General Public License.*</excludedLicense>
                                <excludedLicense>GPL.*</excludedLicense>
                                <excludedLicense>GNU Affero General Public License.*</excludedLicense>
                                <excludedLicense>AGPL.*</excludedLicense>
                                <excludedLicense>Server Side Public License.*</excludedLicense>
                                <excludedLicense>SSPL.*</excludedLicense>
                            </excludedLicenses>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
